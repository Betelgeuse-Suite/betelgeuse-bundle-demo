{
  "name": "mock-repository",
  "version": "0.0.1",
  "description": "The Mock Repository",
  "main": "''",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:compile": "rm -rf ./compiled && mkdir ./compiled",
    "precompile": "npm run clean:compile",
    "compile:json": "node ./node_modules/beetlejuice/.bin/index.js generate-json source --out compiled/MyApp.json",
    "compile:types": "node ./node_modules/beetlejuice/.bin/index.js generate-types compiled/MyApp.json --out compiled/MyApp.d.ts",
    "compile": "npm run compile:json && npm run compile:types"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "beetlejuice": "https://github.com/GabrielCTroia/beetlejuice.git@v0.0.2"
  }
}
